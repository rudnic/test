services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    image: bb-poc-app
    ports:
      - "8888:8080"
    environment:
      - PORT=8080
    configs:
      - qwe

configs:
  poc_config:
    file: qwe
